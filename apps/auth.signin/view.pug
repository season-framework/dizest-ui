.wrapper
    .container
        form.form.pb-1
            a.mb-4.cursor-pointer(href="/" style="display: inline-block;")
                img(src="/resources/images/logo.png" style="width: 160px; display: inline-block;")

            input(type="text" placeholder="Username" ng-model="user.id")
            input(type="password" placeholder="Password" ng-model="user.password" ng-enter="login(user)")
            button#login-button(ng-click="login(user)") LOGIN
            .mt-3
                a.text-white(href="/auth/join") Join
            .mt-2
                small.text-white use admin / 1234 for sign in for demo page.

    ul.bg-bubbles
        li
        li
        li
        li
        li
        li
        li
        li
        li
        li

    {$ wiz.render("component.modal") $}