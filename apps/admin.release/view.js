let wiz_controller = async ($sce, $scope, $timeout) => {
    $scope.history = [];
    $scope.history.push({
        name: "v2022.08.23.1536",
        log: [
            "Releases Info",
            "UI / Core version update on web setting"
        ]
    });
}