doctype 5

html(ng-app="app")
    head
        {$ wiz.theme('default').layout('base').view('header.pug') $}
        
    body.antialiased
        {$ wiz.theme('default').layout('base').view('onload.pug') $}
                
        {$ view $}
